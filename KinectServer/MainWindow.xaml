<Window x:Class="KinectServer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:KinectServer.Converters"
        Title="MainWindow" Height="350" Width="525">
    <Window.Resources>
        <converters:KinectStateConverter x:Key="KinectStateConverter"/>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.Background>
            <ImageBrush ImageSource="Images/background.jpg"/>
        </Grid.Background>
        <StackPanel Grid.Row="0" Grid.Column="0">
            <Label Foreground="White" HorizontalAlignment="Center">Kinect State</Label>
            <TextBlock Text="{Binding KinectState, Converter={StaticResource KinectStateConverter}}"/>
        </StackPanel>
        <StackPanel Grid.Row="1" >
            <Label FontSize="20">Name</Label>
            <TextBox Text="{Binding Player, UpdateSourceTrigger=PropertyChanged}"></TextBox>
        </StackPanel>
    </Grid>
</Window>
